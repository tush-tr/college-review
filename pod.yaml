kind: Pod
apiVersion: v1
metadata:
  name: college-review-pod1
  labels:                                                   
    type: LoadBalancer
spec:
  containers:
    - name: c00
      image: 
      ports:
       - containerPort: 3000
